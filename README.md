# express-api
Expecting the following config.js file:

`const config = {
  database: {  
    host: 'localhost',
    port: 3306,
    user: 'root',
    password: 'root',
    database: 'expressA'
  },
  server: {
    host: 'localhost',
    port: 3000
  }
};`
